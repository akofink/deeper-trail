On the map screen, it is required to press enter twice to travel to a location.

Resolution: fixed by returning normal node completions to a travel-ready map state immediately instead of leaving the runtime in `won` mode after switching to the map scene.
